import { ReactNode } from "react";
interface TitleKeysProps {
    className: string;
    children: ReactNode;
}
export declare const TitleKeys: ({ className, children }: TitleKeysProps) => JSX.Element;
export {};
