import { ReactNode } from "react";
interface KeyboardPromptProps {
    className?: string;
    children: ReactNode;
}
export declare const KeyboardPrompt: ({ className, children, }: KeyboardPromptProps) => JSX.Element;
export {};
